The [keelson.pro](@SITE_URL@) website, as published by this release.

| | |
| --- | --- |
| Site version | `@VERSION@` |
| Published branch | `gh-pages`, tagged `@PAGES_TAG@` |
| Served at | @SITE_URL@ |

The `.tgz` attached below is the `gh-pages` tree at `@PAGES_TAG@`, byte for
byte. Unpack it into any static web server to serve exactly what the live site
serves.
